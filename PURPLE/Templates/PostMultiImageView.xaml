<?xml version="1.0" encoding="UTF-8"?>
<StackLayout  xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
      xmlns:cv="clr-namespace:CarouselView.FormsPlugin.Abstractions;assembly=CarouselView.FormsPlugin.Abstractions"
      xmlns:carousel="clr-namespace:Syncfusion.SfCarousel.XForms;assembly=Syncfusion.SfCarousel.XForms"
             HeightRequest="400"
      x:Class="PURPLE.Templates.PostMultiImageView">
    <StackLayout.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="itemTemplate">
                <Image Source="{Binding Img_name}" 
                       Aspect="AspectFill" />
            </DataTemplate>
        </ResourceDictionary>
    </StackLayout.Resources>
    <CarouselView ItemsSource="{Binding Image}" IndicatorView="indicatorView" ItemTemplate="{StaticResource itemTemplate}">
        
    </CarouselView>
    <IndicatorView x:Name="indicatorView"
                   IndicatorColor="LightGray"
                   IndicatorsShape="Circle"
                   
                   SelectedIndicatorColor="{StaticResource SecondColor}"
                   HorizontalOptions="Center" />
</StackLayout>