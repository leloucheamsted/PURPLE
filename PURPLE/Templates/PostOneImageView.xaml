<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             BackgroundColor="Red"
             HorizontalOptions="Center"
             VerticalOptions="Center"
             
             x:Class="PURPLE.Templates.PostOneImageView">
             
    <ContentPresenter/>
    <StackLayout  HorizontalOptions="StartAndExpand" x:Name="stack" Margin="0,-6,0,0" VerticalOptions="FillAndExpand"  BindableLayout.ItemsSource="{Binding Image}">
        <BindableLayout.ItemTemplate>
            <DataTemplate>
                <Image x:Name="ContenuPost" 
                  
           Source="{Binding Img_name}" 
           
          />
            </DataTemplate>
        </BindableLayout.ItemTemplate>
        <ListView ></ListView>
    </StackLayout>
</StackLayout>