<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PURPLE.Templates.QuestionView">
    <ContentPresenter/>
    <Grid  Margin="0"  MinimumHeightRequest="200"     
                 BackgroundColor="{StaticResource ThirdColor}"
                 VerticalOptions="FillAndExpand"  >
        <Label Text="{Binding Quesion}" TextColor="{StaticResource whiteColor}"
               HorizontalOptions="Center"
              LineHeight="5" CharacterSpacing="2"
               LineBreakMode="WordWrap"
                Padding="10"
               FontSize="25" 
               FontFamily="MetroLight"
               VerticalTextAlignment="Center"
               VerticalOptions="Center"/>
    </Grid>
</StackLayout>